# Configuration for spring-dotenv
# This makes loading .env file mandatory, so application will fail to start if .env file is missing
dotenv.required=true

# Path to .env file (relative to project root or absolute)
dotenv.path=.env